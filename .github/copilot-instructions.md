# LibraryHub

Use JavaScript, React/Vite, Express, and MongoDB. Keep API logic in backend controllers and frontend requests in `src/services/api.js`. Preserve the existing visual language and validate focused slices after edits.
