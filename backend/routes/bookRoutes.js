import {Router} from 'express';import {listBooks,getBook,createBook,updateBook,deleteBook} from '../controllers/bookController.js';import {protect,admin} from '../middleware/authMiddleware.js';const r=Router();r.get('/',listBooks);r.get('/:id',getBook);r.post('/',protect,admin,createBook);r.put('/:id',protect,admin,updateBook);r.delete('/:id',protect,admin,deleteBook);export default r;
