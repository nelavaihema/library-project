import {Router} from 'express';import {listCategories,createCategory,updateCategory,deleteCategory} from '../controllers/categoryController.js';import {protect,admin} from '../middleware/authMiddleware.js';const r=Router();r.get('/',listCategories);r.post('/',protect,admin,createCategory);r.put('/:id',protect,admin,updateCategory);r.delete('/:id',protect,admin,deleteCategory);export default r;
