import {Router} from 'express';import {issue, mine, all, returnBook} from '../controllers/transactionController.js';import {protect,admin} from '../middleware/authMiddleware.js';const r=Router();r.post('/issue',protect,issue);r.get('/my-books',protect,(req,res,next)=>mine(req,res,next,true));r.get('/history',protect,(req,res,next)=>mine(req,res,next));r.get('/',protect,admin,all);r.patch('/:id/return',protect,returnBook);export default r;
