import {Link} from 'react-router-dom';import {ArrowUpRight} from 'lucide-react';const fallbackCover=book=>`https://placehold.co/600x900/d9e5da/35624a?text=${encodeURIComponent(book.title)}`;export default function BookCard({book}){return <article className="book-card"><Link to={`/books/${book._id}`} className="cover" aria-label={`View ${book.title} details`}><img src={book.image||fallbackCover(book)} alt={`${book.title} cover`} onError={e=>{e.currentTarget.onerror=null;e.currentTarget.src=fallbackCover(book)}}/></Link><div className="book-copy"><p className="eyebrow">{book.category?.name||'Collection'}</p><h3>{book.title}</h3><p className="muted">{book.author}</p><div className="card-foot"><span className={book.availableQuantity?'available':'unavailable'}>{book.availableQuantity?`${book.availableQuantity} available`:'This book is not available'}</span><Link to={`/books/${book._id}`} className="icon-link" aria-label="View details"><ArrowUpRight size={18}/></Link></div></div></article>}
