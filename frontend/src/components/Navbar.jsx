import {Link,useNavigate} from 'react-router-dom';import {BookOpen,LogOut,LayoutDashboard} from 'lucide-react';import {useAuth} from '../context/AuthContext.jsx';export default function Navbar(){const {user,logout}=useAuth();const nav=useNavigate();return <header className="nav"><Link className="brand" to="/books"><span className="brand-mark"><BookOpen size={20}/></span>LibraryHub</Link><nav><Link to="/books">Browse</Link>{user&&<><Link to="/my-books">My loans</Link><Link to="/borrow-history">History</Link>{user.role==='admin'&&<Link to="/admin"><LayoutDashboard size={16}/> Admin</Link>}<button className="link-btn" onClick={()=>{logout();nav('/login')}}><LogOut size={16}/> Sign out</button></>}</nav></header>}
