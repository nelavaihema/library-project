import {createContext,useContext,useState} from 'react';import api from '../services/api.js';const AuthContext=createContext();export function AuthProvider({children}){const [user,setUser]=useState(()=>JSON.parse(localStorage.getItem('libraryhub-user')||'null'));async function login(data){const res=await api.post('/auth/login',data);localStorage.setItem('libraryhub-token',res.data.token);localStorage.setItem('libraryhub-user',JSON.stringify(res.data.user));setUser(res.data.user);return res.data}function logout(){localStorage.removeItem('libraryhub-token');localStorage.removeItem('libraryhub-user');setUser(null)}return <AuthContext.Provider value={{user,login,logout}}>{children}</AuthContext.Provider>}export const useAuth=()=>useContext(AuthContext);
