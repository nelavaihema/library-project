import {useEffect,useState} from 'react';import api from '../services/api.js';export default function Dashboard(){const [data,setData]=useState(null);useEffect(()=>{api.get('/admin/dashboard').then(r=>setData(r.data))},[]);if(!data)return <main className="page"><p className="state">Loading dashboard...</p></main>;const s=data.statistics;return <main className="page"><div className="page-title"><div><p className="eyebrow">Operations</p><h1>Good morning, admin.</h1></div><span className="date">Library overview</span></div><div className="stats">{[['Total books',s.totalBooks],['Available copies',s.availableBooks],['Issued now',s.issuedBooks],['Members',s.totalUsers],['Transactions',s.totalTransactions],['Overdue',s.overdueBooks]].map(([l,v])=><div className="stat" key={l}><span>{l}</span><strong>{v}</strong></div>)}</div><section className="panel"><div className="panel-head"><h2>Recent activity</h2><span className="muted">Latest transactions</span></div>{data.recentTransactions.map(t=><div className="activity" key={t._id}><span className="activity-dot"/><div><strong>{t.book?.title}</strong><p>{t.user?.name} · {t.status}</p></div><span className="muted">{new Date(t.createdAt).toLocaleDateString()}</span></div>)}</section></main>}
