import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import toast from 'react-hot-toast';import {useAuth} from '../context/AuthContext.jsx';export default function Login(){const [form,setForm]=useState({email:'',password:''});const {login}=useAuth();const nav=useNavigate();async function submit(e){e.preventDefault();try{const r=await login(form);toast.success('Welcome back');nav(r.user.role==='admin'?'/admin':'/books')}catch(e){toast.error(e.response?.data?.message||'Login failed')}}return <Auth title="Welcome back" subtitle="Sign in to continue your reading journey."><form onSubmit={submit}>{['email','password'].map(k=><label key={k}>{k[0].toUpperCase()+k.slice(1)}<input type={k==='password'?'password':'email'} required value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="primary wide">Sign in</button></form><p className="auth-switch">New here? <Link to="/register">Create an account</Link></p></Auth>};function Auth({title,subtitle,children}){return <main className="auth-page"><div className="auth-aside"><p className="eyebrow">A quieter way to learn</p><h1>Make room for a better book.</h1><p>Keep your next great idea close, from first page to final note.</p></div><section className="auth-panel"><p className="eyebrow">LibraryHub</p><h2>{title}</h2><p className="muted">{subtitle}</p>{children}</section></main>}
