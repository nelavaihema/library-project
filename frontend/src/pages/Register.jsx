import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import toast from 'react-hot-toast';import api from '../services/api.js';export default function Register(){const [form,setForm]=useState({name:'',email:'',password:'',confirm:''});const nav=useNavigate();async function submit(e){e.preventDefault();if(form.password!==form.confirm)return toast.error('Passwords do not match');try{await api.post('/auth/register',form);toast.success('Account created');nav('/login')}catch(e){toast.error(e.response?.data?.message||'Registration failed')}}return <main className="auth-page"><div className="auth-aside"><p className="eyebrow">LibraryHub</p><h1>Your next chapter starts here.</h1><p>Build a reading life that is easy to return to.</p></div><section className="auth-panel"><p className="eyebrow">Get started</p><h2>Create your account</h2><form onSubmit={submit}>{[['name','Full name','text'],['email','Email','email'],['password','Password','password'],['confirm','Confirm password','password']].map(([k,l,t])=><label key={k}>{l}<input type={t} required minLength={k==='password'?6:undefined} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="primary wide">Create account</button></form><p className="auth-switch">Already a member? <Link to="/login">Sign in</Link></p></section></main>}
